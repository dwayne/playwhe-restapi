source :rubygems

gem 'sinatra'
gem 'thin'

gem 'json'

gem 'data_mapper'
gem 'dm-sqlite-adapter'
gem 'dm-ar-finders'

group :development do
  gem 'net-ssh' # needed for deployment, see rake:deploy
end
